# API desplegada. La usan `expo export`, los builds Release, EAS Build y
# `npm run start:prod-api` / `npm run ios:prod-api`.
#
#   cp .env.production.example .env.production

EXPO_PUBLIC_STAGE=prod

EXPO_PUBLIC_API_URL=https://tu-api.example.com/api
